import TOKEN from './utils'



const retrieveQuestion =  (category,dificulty,type) {

    const TOKEN = TOKEN ;

    // Type: multiple // boolean
    // category: is the ID

    const URI = `https://opentdb.com/api.php?amount=10&category=${category}&difficulty=${dificult}&type=${type}`

    
} 